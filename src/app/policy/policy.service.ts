import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class PolicyService {

  constructor() { }

  setCompany(companyId: number, roomTypes: string[]): void {
  }

  setEmployee(employeeId: number, employeeRoomTypes: string[]): void {

  }
}
